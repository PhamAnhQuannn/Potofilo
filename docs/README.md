# Potofilo

Portfolio cá nhân — Phạm Anh Quân.

Tài liệu nội bộ (cho AI + dev) ở `brain/`. Thư mục `docs/` này dành cho người ngoài.

## Cấu trúc
- `app/` — sản phẩm
- `content/projects/` — dữ liệu dự án
- `public/` — asset
- `brain/` — não bộ nội bộ
- `.claude/` — cấu hình Claude Code (rules, skills, agents, commands)

Stack + nội dung: xem `brain/decisions/`.
