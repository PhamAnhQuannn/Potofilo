# STATE — Hiện tại

- **Cập nhật:** 2026-08-19

## Đang làm
Xong REDESIGN (decision 0004): bento 1 viewport + vụ nổ khai sinh trang. Thay thế bản GĐ3 cũ (hành tinh→navbar).

## Dừng ở đâu
Webapp tại `http://localhost:4001/` (server nền ID bprrwo509). Files: `index.html` (bento 7 ô), `styles/main.css` (grid + overlay), `hero-sphere.js` (5 phase khai sinh), `dust-layer.js`, `bento.js` (mới). Cú pháp 4 JS OK, route 200, wiring + CSS đủ. CHƯA xem tận mắt/tinh chỉnh. Nội dung ô còn placeholder `[...]`. `app/intro/` (prototype cũ) không dùng — để tham khảo. `site.js` (nav cũ) còn include nhưng no-op — có thể bỏ sau.
- **Intro gate:** `sessionStorage cosmic-intro-seen` — intro chạy 1 lần/phiên; thêm `?intro` ép chạy lại để test (report 0011). Giữ `history.scrollRestoration='manual'` + `scrollTo(0,0)` khi boot (tránh scroll-restore bị coi là skip).
- **Lời chào intro** (report 0010, sửa ở 0011): "HELLO → I'M QUAN" DƯỚI RÌA quả cầu (+40px), fix bug `place()` NaN (thêm `camera.updateMatrixWorld` + isFinite guard). Timeline TỶ LỆ void+charge (9%/39%/73%/88%). Hút vào TÂM cầu (bezier 0.55,0,1,0.45), hết trước flash.
- **State machine mới** (report 0011): `void→charge→explode→planets→cascade→crystallize→alive`. Desktop dur (mobile ×0.72): 1.5/1.8/1.4/1.6/1.5/1.3 ≈ 9.1s. `planets` = 7 hành tinh SỐNG (tự quay quanh trục nghiêng + trôi elip + thở + glow accent). `cascade` = nổ dây chuyền 7 cú (breakAt=k×0.18, sóng mini + glow lóe, KHÔNG shake, drag 0.90). `crystallize` = bay quãng ngắn từ hành tinh vào ô, daisy stagger, dust handover ở 70%. Code trong `hero-sphere.js` (methods `_enterPlanets/_planets/_enterCascade/_cascade/_breakPlanet/_buildPlanetFX`). **CHƯA xem tận mắt** (env không mở WebGL) — cần Quân duyệt cảm quan + tinh chỉnh.
- **Hiến pháp thiết kế v1** (decision 0005): `brain/context/design-constitution.md` — 5 trụ + floating + bất biến. CLAUDE.md luật cứng #0: đọc + 6 câu hỏi trước mọi thay đổi thị giác.
- **Nebula Structure** (report 0014): `dust-layer.js` thêm value-noise/fBm → sinh 3 texture tinh vân (trái/phải-dưới/dải) + lõi thiên hà (bake, sáng nhất nền) trong idle tuần tự sau ALIVE; vẽ 'screen' lên cùng canvas trước stars. Cụm sao 3 tâm (gauss σ4vh, ấm) thay rải đều; 2 hero neo rìa cụm. Gradient nebula CSS cũ giảm 40% (lớp sương sau). **Nợ audit:** reduced-motion CHƯA vẽ texture (canvas stub) — sửa ở BƯỚC 2. **CHƯA xem tận mắt.**
- **Hiến pháp v2.0 "MỘT hệ"** (decision 0018): triết lý không còn nền/trang — MỘT hệ. Tu chính Trụ 1 (vụ nổ sinh toàn hệ + thiên thể artwork), Trụ 2 (tile shade LIGHT_DIR + khí hậu ánh thứ cấp), Trụ 3 (tiểu khí hậu + ngoại lệ rocky ấm), Trụ 4 (bảng lớp mây+Kepler, xóa "40px", mặt tối anchor sau grid), Trụ 5 (sự kiện-nội-dung=8), **Trụ 6 mới** (định luật Kepler tham số), phụ lục giải phẫu 8 thiên thể. **Đang thi công** 7 block. **BLOCK 1 asset+mây XONG:** `dust-layer.js` — `tryLoadAssets` load `./assets/celestial/*.webp` (anchor/ice/rocky/moon/cloud-back/mid/front), thiếu→procedural fallback (onerror, không lỗi); ảnh override procedural (anchor ảnh=tĩnh, thiếu=sọc quay). `makeCloudTex` mây teal fBm + `drawClouds` 3 tấm parallax 0.02/0.05/0.11 (front vệt mỏng) lớp 1. README `app/assets/celestial/` (danh mục + nén ≤1.4MB, sáng phải-trên). **BLOCK 4 (bản an toàn) XONG:** mây fade-in SO LE khi vào ALIVE (`aliveT` echo e0/e1/e2, gần sáng trước) + hệ hình thành qua texture idle-load. reduced: `aliveT=1` (mây đủ). **KHÔNG đụng hero-sphere intro** → regression greeting/scroll-skip/cascade/gate nguyên. **Hoãn** (backlog, rủi ro WebGL): mây VOID 0.06 + echo theo sóng explode + 5% hạt ngưng tụ cross-fade. **→ 7 BLOCK "MỘT hệ" xong (bản procedural/an toàn), chờ ảnh thật + calibration ?tune.** **ÁNH SÁNG v2 "Chiaroscuro" XONG:** (V1) lõi RESPONSIVE `updateCorePos()` — cánh rộng ló trọn / hẹp ló sau góc Featured; LIGHT_DIR + Kepler focus + cụm sao (c) bám `corePos()` (gom hết hardcode 0.72W/0.30H). (V2) bloom `'lighter'` cho sao hero/glint/đèn/quầng khí hậu/lõi+xung/embers/sao băng (reset source-over ngay). (V3) `heroGlowTex` sprite mềm thay 2-arc + glint 8-cánh (diamond 3 sao). (V4) star flare 8–15s (op×3/size×1.6/glint tạm) + twAmp rand(0.05–0.18). (V5) khí hậu thấm viền tile (`--climate-tint` mỗi 0.5s, CSS color-mix). (V6) TUNE nhóm **"Ánh sáng"** (bloom/core/starMul/flare/cursorOp/glint/tileTint) — **giải nợ B6 thiếu nhóm + backlog khí-hậu-viền-tile**. main.css tile border-color color-mix. Floor tối KHÔNG đổi. **CHƯA xem tận mắt.**
- **B4-KHAI-SINH (bản bridge)** (commit 895d36a): flag `?b4` (default ON, off=cũ 100%). Hệ nền HÌNH THÀNH TRONG intro qua bridge `CosmicDust`: `startPreAlive`(VOID mây 0.06)→`pregenTextures`(diệt pop)→`cloudLight` echo(explode 0.25/0.45/0.70)→`reveal` distant/rocky/ice(cascade stagger)→anchor+`coreEnergy` ramp(crystallize). Đóng nợ audit Trụ 1 phần "nhịp chết+pop+fade vô cớ". hero-sphere chỉ THÊM `b4tick` (không đụng buffer hạt tile). **HOÃN polish:** choreography hạt sys WebGL (proto-disk spin) + TUNE "Khai sinh". Regression code-level PASS (b4=off provably cũ). **CHƯA xem tận mắt.**
- **4 SỰ KIỆN fix intro** (E2 d968ae1 · E3 8454e48 · E1 2b3d12c): **E2 keystone** — `_breakPlanet` thiếu `P.broken=true` → cascade nguyên khối, gọi lại mỗi frame; 1 dòng chữa cascade-vỡ-thật + vòng-đứng-im + nhãn-sống-dai. **E3** vòng cascade `RingGeometry 0.9→0.985` (mảnh). **E1** `coreEnergyV 0.6→0.1` (lõi lờ mờ trong VOID, cứu light-echo). **E4** không code (E2 tự chữa nhãn). E2/E3 áp cả b4 on/off; E1 chỉ B4ON. node --check OK.
- **Sửa giật quả cầu mở màn** (report 0020): Quân báo quả cầu giật ~0.3s đầu (VOID) trước HELLO. 3 Sonnet điều tra + 2 Sonnet debate → Opus phân xử (đọc code). 3 fix: (#1 gốc) `dust-layer.js` tách `scheduleClouds()` (chỉ 3 mây, gọi ở startPreAlive/VOID) khỏi `scheduleTextures()` nặng (nebula/bands 2M px/shade/planet — dời sang pregenTextures ở CHARGE, che bởi nổ, xong trước ALIVE); cờ riêng `cloudScheduled`+`texScheduled` chống double-bake; enterAlive gọi cả 2 (b4=off nguyên vẹn). (#2) `hero-sphere.js` thêm `this.renderStatic()` cuối init = warm-up compile shader ở load-time (KHÔNG dùng renderer.compile — né cược THREE r128). (#3) dt=0 frame đầu ở CẢ 2 loop (hero start + dust step/startLoop sentinel lastT=0), GIỮ clamp 0.05 (không siết 0.033 — tránh slow-mo charge). node --check OK. 2 issue mở: 0001 Safari chunk bake, 0002 buildParticles async. **CHƯA xem tận mắt.**
- **Chunk bake texture — hết giật "I'M QUAN"** (report 0022): loop fix vòng 1. Đo 50fps tới planets thấy cú đơ **~250ms ở đầu CHARGE (= lúc I'M QUAN)** do `scheduleTextures` bake nặng (bandsTex 2048×1024 + nebula×3 + planet×3) chạy trọn 1 lần — GPU-agnostic. Workflow 3 Sonnet (Node bench: bandsTex ~230ms khớp browser 240-283ms; QA; debate row-chunk) → Opus plan. Fix `dust-layer.js`: primitive `makeRowBaker` (vẽ ImageData theo lát hàng + atomic assign), đổi makeNebulaTexture/makeBandsTex/makePlanetTexture → *Baker (planet: chunk Pass1 → finalize Pass2-5 gradient → gán atomic; snapshot LIGHT_DIR cho nebula-left bias tránh tách tint khi resize), viết lại `scheduleTextures` = driver deadline-aware (`timeRemaining()>3`, rows/lát hiệu chỉnh <4ms) + Safari cap; giữ galaxyCore/shadeTex đồng bộ (gradient rẻ). **Đo lại: KHÔNG còn longtask>20ms trong intro 0-7s** (trước 240-283ms) qua 3 run → **đạt đích loop**. Effects pixel-identical (frame verify I'M QUAN/7 planets/ALIVE nebula+planet OK), no console error, node --check OK. **Đóng issue 0001** (Safari chunk — nay chia nhỏ mọi bake + cap Safari). Số đo scratchpad. **CHƯA xem máy thật.**
- **Đo thật + hoàn thiện chống giật intro** (report 0021): Quân quay phim 50fps → vẫn giật lúc mở. Lần này ĐO bằng số (Playwright instrument: longtask/frame-gap/FOUC/three.js load; Node bench fBm). 3 Sonnet đo+QA+debate → Fable synthesis. Chốt **2 lỗi thật tách biệt**: (RC1) 3 bake mây `makeCloudTex` trong VOID ~170ms/3 cú → rớt frame (GPU-agnostic, KHÔNG có webp nên fallback luôn chạy); (RC2) FOUC — three.min.js không defer → chớp bento ~28–57ms. **Sửa:** (1) `index.html:166` +`defer` (đo lại: warm-cache flash ~35ms KHÔNG giảm — bị stylesheet head gate, nhưng xoá cold-cache flash → vẫn giữ); (2) `dust-layer.js` `makeCloudTex`→`makeCloudTexBaker` vẽ-theo-lát tôn trọng `timeRemaining()` + gán atomic khi xong (không pop) + cap cứng Safari; `scheduleClouds` viết lại. Đo sau: **hết long task bake mây trong VOID, gaps về ~16.7ms**. node --check OK, chạy thử không lỗi console, ALIVE render đủ mây (chụp kiểm). Issue **0002 ĐÓNG** (buildParticles đo ~2.1ms, không đáng chunk). Mở issue **0003** (diệt nốt FOUC — cần Quân duyệt vì đụng no-JS invariant), **0004** (webp mây bake sẵn — chờ Quân quyết asset-pipeline), **0005** (warm ring shader — điều kiện: Quân thấy giật lúc nổ). Issue 0001 vẫn mở (nay có template row-chunk tái dùng). Dụng cụ/số đo ở scratchpad. **CHƯA xem tận mắt máy thật.**
- **Phóng to lời chào intro** (report 0019): Quân muốn "HELLO / I'M QUAN" to + rõ + dễ nhìn hơn. 2 Sonnet agent đo + đánh giá → Opus synthesis, Quân chọn A+B. `main.css` #cosmic-greet: font-size 16px→`clamp(22px,3.2vw,38px)`, weight 300→600, letter-spacing 0.35em→0.3em, color #FFF4D6→#FFFFFF, text-shadow 3 lớp (glow nóng + bóng đen mỏng tách nền), thêm `padding:0 2.5em` + `background` radial-gradient halo tối `rgba(5,5,16,0.5)` (tách chữ khỏi hạt sáng); `.cg-char.cg-in` opacity 0.85→1. `hero-sphere.js`: suck opacity `0.85*(1-e)`→`(1-e)` (đồng bộ, hết giật), fade container `opacity=0` khi suck xong + trong destroy() (diệt halo trơ ~0.4s / đường skip), reset opacity lúc init. Đúng hiến pháp (Trụ 3 màu NÓNG; halo là nền element sau chữ, không phá bất biến "hạt đè chữ op>0.4"). node --check OK. **CHƯA xem tận mắt.**
- **Làm mềm viền khí quyển** (report 0018): Quân báo "viền trắng gắt cạnh phải mọi hành tinh". Là viền khí quyển cạnh sáng (LIGHT_DIR trên-phải) — hiến pháp Trụ 2 + phụ lục bắt buộc, KHÔNG bỏ. Sửa cho ĐÚNG hiến pháp: `dust-layer.js` 3 chỗ rim đổi `#CDE3FF` (xanh-lạnh, sai Trụ 3) → `#FFF4D6` (NÓNG); bỏ đường mảnh gắt op 0.5 (planet+anchor: `rim 0.014/0.10 + 0.007/0.16`); moon hạ `0.016/0.22 + 0.009/0.38` (vẫn không thành chấm đen); distant op 0.7→0.42 giữ 1px. Không đổi hình học/hướng/clip/terminator. node --check OK. **CHƯA xem tận mắt.**
**AUDIT tổng XONG** (`docs/audit-report.md`): đối chiếu hiến pháp v2.0.1 — hầu hết ✅; **Trụ 1 ⛔ treo** (B4 thiếu light-echo+ngưng tụ); cần-quyết CÒN: thả ảnh, điền placeholder. **DONE:** dọn legacy `app/intro/`+`site.js` (xóa, git history giữ) · TUNE nhóm "Ánh sáng" (V6). Không sửa code bắt buộc. Perf/Lighthouse/screenshot CHƯA đo (env không render — chỗ điền sẵn trong report). **BLOCK 5 (duyệt: đạt-bằng-kiến-trúc)** — HẠ CẤP: luật "rim anchor ngoài grid" → HƯỚNG DẪN CALIBRATION (không enforce code; tile `background` đục che canvas z0 → contrast chữ an toàn sẵn); "distant xuyên khe" → backlog polish. Thứ tự còn lại: **B1 (assets+mây 3 tấm) → B4 (intro echo)**. **BLOCK 6 Tile-thiên-thể XONG:** `main.css` — tile rim gradient góc trên-phải theo LIGHT_DIR (bg 2 lớp), box-shadow đổ lệch DƯỚI-TRÁI; `.cosmic-ping` glow. `dust-layer.js` — xung lõi quét tile: `buildPingTiles`/`pingTilesSweep` (sóng lan theo khoảng cách từ lõi, add `.cosmic-ping` 0.6s stagger, năng lượng 8). Hoãn: khí hậu-viền-tile bake (planet trôi Kepler → gán tĩnh vô nghĩa). **BLOCK 3 Khí hậu màu XONG:** `dust-layer.js` — `CLIMATE[3]` (anchor teal / ice tím / rocky **gỉ ẤM** ngoại lệ Trụ 3) bám vị trí Kepler(t); `drawClimateHalos` quầng radial tint; bụi lerp màu ≤30% khi vào vùng (dùng CHUNG distance với slingshot). Tham số → TUNE nhóm "Khí hậu" (radius×D/halo/dustLerp). Nebula hue-shift bake hoãn (planet trôi Kepler). **BLOCK 2 Kepler XONG** (report 0017): `dust-layer.js` — `keplerPos(a,period,phase)` giải tích (solveKepler Newton, tiêu điểm lõi 0.72W/0.30H, dẹt+tilt -24°); 4 thiên thể distant/rocky/ice/anchor bán trục 1:1.7:2.9:4.9, chu kỳ 240/420/540/1200s (v∝1/√r), vị trí = hàm time (throttle-safe). Trăng quay quanh anchor 150s + **z luân phiên** (cung xa sau / gần trước = transit). Distant = silhouette+rim procedural. ice/rocky bake (bỏ mobile). Bụi slingshot quanh anchor (1.6×). Tham số → TUNE nhóm "Quỹ đạo". CHƯA đặt anchor sau grid (B5). **CHƯA xem tận mắt — cần tinh chỉnh phase/tilt qua ?tune.**
- **TUNE panel XONG:** `app/tune.js` (`window.TUNE.register(group,key,def)` + panel `?tune` slider live + Copy/Reset + localStorage). Include trước dust-layer. Còn: Kepler (B2, chờ duyệt hiến pháp) → khí hậu (B3) → alive (B5) → tile (B6) → intro echo (B4) → assets (B1). Ảnh dưới `app/assets/celestial/` (chưa có → procedural fallback, wire ở B1).
- **Living Galaxy** (report 0016 + decision 0017): KHỐI 0 fix render (sọc cong theo vĩ độ, limb rõ, trăng không đen). KHỐI A tương tác chuột (parallax dãn phổ 0.03–0.22 + grid ≤6px, đèn con trỏ sprite radial lerp 0.06 fade 0.8s, tile tilt 3D ±5° perspective, 1 pointermove listener). KHỐI B nhịp sống tự thân: hành tinh tự quay (tách `makeBandsTex` seamless + `makeShadeTex` tĩnh, `drawPlanetRot` scroll 140s), xung lõi 25–45s, cửa sổ sáng dải 28s+4s, twinkle không gian, trăng trôi 20px/phút. **reduced-motion**: bỏ stub → `renderReducedStatic` vẽ 1 khung tĩnh (tinh vân+hành tinh+sao) rồi dừng — trả nợ bất biến. Tu chính hiến pháp: parallax v1.2 (Trụ 4) + năng lượng người-dùng trần 30 (Trụ 5). **CHƯA xem tận mắt.**
- **Tu chính hiến pháp Trụ 4** (decision 0016): tách "sao & bụi" → sao xa (lớp 1b, SAU thiên thể, bị hành tinh che) + bụi/embers (lớp 3, trước). Z-order vẽ: nebula < sao xa < thiên thể < bụi < tile. `dust-layer.js` đổi thứ tự: `drawNebulaBg` → stars → `drawCelestials` → dust/embers. Ràng buộc rìa hành tinh cách grid ≥40px (`gridLeft-40-R` / moon `gridRight+40+R`). `brain/context/design-constitution.md` cập nhật v1.1 (docs/ bản gốc nay lệch). Backlog polish: viền khí quyển pha #E84A8A.
- **Realistic Planets** (report 0015): `dust-layer.js` + `LIGHT_DIR` (nắng từ lõi 0.72W/0.30H), `makePlanetTexture` (bands fbm bóp cầu / limb darkening / terminator -LIGHT_DIR lệch 15% / viền khí quyển 3 arc / specular). Hành tinh khí 1024² (mobile 640²) góc trái-dưới cắt 40%, trăng 256² phải-trên (bỏ mobile) — sinh idle SAU nebula tuần tự, vẽ layer2 (parallax 0.04, trôi 90s) TRƯỚC stars. Core parallax →0.04. Tinh vân trái bias sáng về LIGHT_DIR. Fix galaxy core alpha>1. **Mâu thuẫn spec↔hiến pháp** thứ tự lớp: theo hiến pháp (thiên thể dưới sao). **Nợ:** reduced-motion static (BƯỚC 2). **CHƯA xem tận mắt.**
- **Galaxy Boost** (report 0013): nâng độ hiện diện MÀU TĨNH của ALIVE ~2.5–3× (GIỮ mọi timing/biên độ). `main.css`: `--bg`→#07071A, `.vignette` (4 góc tối), `.galaxy-band` dải chéo -24° (galaxy-breathe 30s ±25%), tinh vân 3 cụm đa lớp (8 div, opacity 0.14–0.26, tâm ở hai cánh), `.tile` nền gradient tối + border color-mix accent 28% + box-shadow rim/bóng sâu/glow accent (glow qua box-shadow vì `.tile` overflow:hidden clip `::before`). `dust-layer.js`: sao 3 cấp (faint 200/mid 50/hero 9, hero chỉ hai cánh + halo 2 lớp + glint), 40% faint dọc dải, embers 10→14 op 0.14–0.24. reduced-motion: tinh vân+dải tĩnh GIỮ MÀU. **CHƯA xem tận mắt.**
- **Nhãn hành tinh + Vũ trụ nền ALIVE** (report 0012): (A) 7 nhãn `.planet-label` text từ `.tile__label`, màu accent, bám tâm hành tinh, "thổi bay" khi vỡ (`makePlanetLabels` trong hero-sphere.js). (B) ALIVE hết tĩnh: 4 tinh vân CSS thở (gate `body.cosmic-alive`), `dust-layer.js` refactor 4 nhóm 1 canvas (dust/stars/embers/meteor) + parallax chuột (px/py lerp 0.04) + bento parallax ±3px + sao băng ~25–40s + seedStars nối bầu trời từ sao WebGL. Ô bento lơ lửng lệch pha (`tile-float` + `--float-dur/delay`). Kích hoạt ở `finishIntro`/nhánh seen/reduced. `?debug` log frame-time. reduced-motion → tĩnh sạch. **CHƯA xem tận mắt.**

## Next step
1. Quân mở `http://localhost:4001/?intro` (ép chạy lại intro) duyệt 7 nghiệm thu report 0011: lời chào dưới rìa cầu + hút vào tâm hết trước flash; sau big bang thấy 7 hành tinh SỐNG ~1.6s; dây chuyền vỡ đúng thứ tự có sóng mini màu riêng, không flash trắng/không shake; hạt mỗi hành tinh kết tinh vào đúng ô; tổng ≤9.5s; scroll-skip mọi phase; không console error.
2. Tinh chỉnh cảm quan (vừa khít viewport, độ mượt kết tinh, thời lượng, mật độ bụi).
3. Điền nội dung thật (placeholder trong các ô) + ảnh.

## Blocker
- Chờ Quân duyệt trên trình duyệt + tinh chỉnh + điền nội dung.
